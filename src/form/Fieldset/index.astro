---
export type Props = {
  legend?: string;
};

const { legend } = Astro.props;
import "./index.css";
---

<fieldset class="sb-fieldset">
  {legend ? <legend>{legend}</legend> : null}
  <slot />
</fieldset>
