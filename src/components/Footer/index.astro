---
import Microdata from "../Microdata/index.astro"
import type { FooterProps as Props } from "../../types"
import mapAttributes from "../../utilities/mapAttributes"
import "./index.css"

const {
  microdata = {},
  properties,
  role = "contentinfo",
  ...attrs
} = Astro.props

const wrapperAttrs = mapAttributes(attrs, ["sb-footer"])

const useMicrodata = Boolean(import.meta.env.USE_MICRODATA)
---

<footer
  aria-label="footer"
  {...wrapperAttrs}
  {role}
>
  {
    useMicrodata ? (
      <Microdata
        {...microdata}
        {properties}
      />
    ) : null
  }
  <slot />
</footer>
