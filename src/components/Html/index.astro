---
import type { HTMLAttributes } from "astro/types";

export type Props = HTMLAttributes<"html">;

const {
  dir = "ltr",
  lang = "en",
  prefix = "og: https://ogp.me/ns#",
  ...attrs
} = Astro.props;
---

<html {...attrs} {dir} {lang} {prefix}>
  <slot />


</html>
