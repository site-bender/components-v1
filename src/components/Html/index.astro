---
import type { HTMLAttributes } from "astro/types"

export type Props = HTMLAttributes<"html">

const { dir = "ltr", lang = "en", ...attrs } = Astro.props
---

<html {...attrs} {dir} {lang}>
	<slot />

</html>
