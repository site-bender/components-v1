---
import type { NumberProps as Props } from "../../types"
import Number from "../Number/index.astro"

const { type: _type, value: val, ...attrs } = Astro.props

const value = Math.round(
	typeof val === "number" ? val : parseInt(String(val), 10)
)
---

<Number {...attrs} type="Integer" {value} />
